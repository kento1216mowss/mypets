<div class = "grid">
     <div class = "top">
         <% if @user.image.attached? %>
         <%= image_tag @user.image,size: '250x250', class: "user-img" %>
         <% else %>
         <%= image_tag 'dog.jpeg',size: '250x250', class: "user-img"%>
         <% end %>
     </div> 
     <div class = "grid-center">
       <div class = "user-infomation">
            <%= @nickname %>
          <% if user_signed_in? && @user.id == current_user.id %>  
              <%= link_to "編集", edit_user_path(current_user) %>
          <% end %>
        </div>
        <div class = "following">
           <% if user_signed_in? %>  
            <% unless @user == current_user %>
               <% if current_user.following?(@user) %>
                   <%= link_to 'フォロー中', unfollow_path(@user.id), method: :POST %>
               <%else %>
                   <%= link_to 'フォローする', follow_path(@user.id), method: :POST %>
               <% end %>
            <% end %>
           <% end %>
        </div> 
         
          
        <div class = "following-box">
          <p><%= "#{@user.follower.count}フォロー" %></p>
          <p><%= "#{@user.followed.count}フォロワー" %></p>
        </div>  
     </div>
 
     <div class = "bottom">
         <p>投稿一覧</p>
        <% @tweets.each do |tweet| %>
            <div class = "user-tweet-image">
              <image src = "<%= tweet.image %> "width= "300" height="300"></image>
            </div>
        <% end %>
      </div>  
  
  
  
</div>